/** Row from the `credit_card_tiers` catalog. */
export class CreditCardTier {
  public constructor(
    public readonly id: string,
    public readonly key: string,
    public readonly displayNameEs: string,
  ) {}
}
